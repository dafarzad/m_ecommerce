import {
  Grid2x2Plus,
  LayoutDashboard,
  Logs,
  Store,
  TicketPercent,
} from "lucide-react";

export const icons = {
  dashboard: LayoutDashboard,
  store: Store,
  order: Logs,
  categories: Grid2x2Plus,
  coupon: TicketPercent,
};
